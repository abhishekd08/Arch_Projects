const bodypix = require('@tensorflow-models/body-pix');
const cv = require('opencv4nodejs');
const fs = require('fs');
fs.readFile('img.jpg', function(err, img) {
    if(err){
        console.log(err);
    }else{
        
    }
});

